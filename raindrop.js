class Raindrop {
 constructor(x,y,w,h){
     var options = {
         'restituition':0.8,
         'friction':0,
         'density':0
     }
     this.width = width;
     this.height = height;
     World.add(world,this.body);
 }   
}